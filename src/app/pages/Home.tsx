import React, {Component} from 'react';
import { Login } from '../../core-ui/views';

export default class Home extends Component {
    render () {
        return (
            <Login></Login>
        );
    }
}
